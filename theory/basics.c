#include <stdio.h> //header file library

int main() // function
{
    printf("My Setup is complete\n");
    // every c statment ends with a semicolon
    printf("we use backslash-n to print in a new line \n\n");
    // two backslash-n {\n\n} will create a blank line
    printf("Here is the break in above line");
    // \n is called as escape sequence
    printf("Backslash-t  \t result is the space between the starting 2 words \n");
    printf("\\ - double backslash adds a backslash in the output \n");
    printf("\" backslash and double quote together is used to add a double qoute in the output");
    return 0;
}